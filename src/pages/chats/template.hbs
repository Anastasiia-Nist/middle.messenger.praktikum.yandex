{{#> layout-page title=title}}
  <p>{{stubText}}</p>
  {{> ui-list items=messages}}
  {{> ui-form formClass=form.formClass name="chat-message" fields=form.fields actions=form.actions}}
  <a href="#/">На главную</a>
{{/layout-page}}
