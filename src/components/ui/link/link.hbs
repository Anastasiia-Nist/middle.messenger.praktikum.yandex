<a class="link {{linkClass}}" href="{{href}}" aria-label="{{ariaLabel}}">
  {{#if (eq variant 'back')}}
    <span class="settings-sidebar__back" aria-hidden="true"></span>
  {{else}}
    {{text}}
  {{/if}}
</a>
