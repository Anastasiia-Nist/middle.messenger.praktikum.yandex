{{#if (eq type 'icon')}}
  <span class='avatar avatar_type_icon {{avatarClass}}' aria-hidden='true'></span>
{{else}}
  <button class='avatar {{avatarClass}}' type='{{type}}'>
    <span class='avatar__overlay'>
      {{overlayText}}
    </span>
  </button>
{{/if}}
