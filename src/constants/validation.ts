export const VALIDATION_PATTERNS = {
  NAME: /^[A-ZА-ЯЁ][a-zа-яё]+(?:-[A-ZА-ЯЁ][a-zа-яё]+)*$/,
  LOGIN: /^(?=.*[a-zA-Z])[a-zA-Z0-9_-]{3,20}$/,
  EMAIL: /^[a-zA-Z0-9._%+-]+@[a-zA-Z]+[a-zA-Z0-9.-]*\.[a-zA-Z]{2,}$/,
  PASSWORD: /^(?=.*[A-Z])(?=.*\d).{8,40}$/,
  PHONE: /^\+?\d{10,15}$/,
} as const

const PASSWORD_MESSAGE =
  'Пароль должен содержать от 8 до 40 символов, минимум одна заглавная буква и одна цифра'

export const VALIDATION_MESSAGES = {
  first_name:
    'Некорректное имя. Допустимые символы: латиница или кириллица, первая буква заглавная, без пробелов и цифр, допустим только дефис',
  second_name:
    'Некорректная фамилия. Допустимые символы: латиница или кириллица, первая буква заглавная, без пробелов и цифр, допустим только дефис',
  display_name: 'Имя в чате не может быть пустым',
  login:
    'Некорректный логин. Допустимые символы: от 3 до 20 символов, латиница, не только цифры, без пробелов, допустимы дефис и подчёркивание',
  email:
    'Некорректный email. Допустимые символы: латиница, цифры и спецсимволы, обязательны @ и точка, между ними должны быть буквы',
  password: PASSWORD_MESSAGE,
  oldPassword: 'Старый пароль не может быть пустым',
  newPassword: PASSWORD_MESSAGE,
  phone: 'Телефон должен содержать от 10 до 15 цифр',
  message: 'Сообщение не должно быть пустым',
} as const
